"""Дообучение LoRA по тому же конвейеру, что и инференс.

Между машинами — pipeline parallelism: стадия держит свой срез слоёв, вперёд
по конвейеру идут активации, назад — их градиенты. Обучаемые параметры (LoRA)
живут по слоям, то есть у каждой стадии свои, и между узлами не ходит ни один
градиент весов. Внутри машины — тот же торч-движок, что у инференса: срез
слоёв на картах узла.

Один код для любого числа стадий: при N=1 голова сама и первая, и последняя,
и сообщения между стадиями не уходят в сеть.

    lora.py       адаптеры на срезе и их сохранение в формате PEFT
    forward.py    батч с паддингом и маской через срез, с чекпоинтингом
    stage.py      стадия в режиме обучения: forward / backward / step / save
    data.py       диалоги → токены и метки; микробатчи
    transport.py  как голова говорит со стадиями (в процессе или по сети)
    head.py       тренер: расписание GPipe, эпохи, метрики, сборка адаптера
"""
